# =============================================================================
# Secrets — NUNCA commitar
# =============================================================================
.env
.env.local
.env.*.local

# =============================================================================
# Python — venv + caches
# =============================================================================
.venv/
venv/
env/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# =============================================================================
# Vector store local — regenerável via `python scripts/ingest_kb.py`
# (~6 MB, fica fora do repo pra não inflar; primeira execução demora ~30s)
# =============================================================================
.chroma/
data/chroma/

# =============================================================================
# Planilha do precificador — fora do repo (contém metadados Excel com nome
# de membro do grupo). O conteúdo numérico já está versionado em
# `src/insurmind/quote_tables.py` (gerado via scripts/import_precificador.py).
# Bruno mantém uma cópia local; regerar `quote_tables.py` exige a planilha.
# =============================================================================
Precificador_Seguro_Autom*.xlsx

# =============================================================================
# Modelos / caches de embedding cacheados (~500MB) — ficam em ~/.cache/
# Mantido aqui pra documentar intent caso algo escape do user dir
# =============================================================================
.huggingface/

# =============================================================================
# Streamlit
# =============================================================================
.streamlit/secrets.toml

# =============================================================================
# Logs temporários e ad-hoc
# =============================================================================
*.log
ui-test*.log
ui-test*.err

# =============================================================================
# Next.js / Node (a partir da Sprint 3 — frontend web/)
# =============================================================================
web/node_modules/
web/.next/
web/out/
web/build/
web/.vercel/
web/coverage/

# =============================================================================
# OS / IDE
# =============================================================================
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# Arquivos de lock do Microsoft Office (aparecem enquanto o arquivo está aberto)
~$*
